# Wonder Inventory DQ — backend (runs on Python 3.9+)
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.26
python-dotenv>=1.0

# Optional — only needed when DATA_SOURCE=bigquery (kept out of the base install so
# fixtures mode runs with no GCP dependency). Install with:
#   pip install "google-cloud-bigquery>=3.17"
