# Wonder Inventory DQ — backend runtime deps (Python 3.11 in prod; runs on 3.9+ locally)
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.26
python-dotenv>=1.0

# Schema migrations (prod/Postgres) — `alembic upgrade head` owns the prod schema.
alembic>=1.13
# Postgres driver (Cloud SQL). SQLite (local/dev) needs no driver.
psycopg[binary]>=3.1

# BigQuery — required when DATA_SOURCE=bigquery (prod). Harmless to install in fixtures mode.
google-cloud-bigquery>=3.17

# In-process daily scheduler (localhost stand-in for Cloud Scheduler when SCHEDULER_ENABLED=true).
# At go-live, Cloud Scheduler drives /api/run instead and this stays disabled.
apscheduler>=3.10,<4

